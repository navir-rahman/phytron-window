#include <stdio.h>
#include <string.h>
int main()
{
    /* code */
    // char a[6];
    // scanf("%s",&a);

    // printf("%s\n",a);

char a[18];
// gets(a);
fgets(a,18  ,stdin);
printf("%s",a);
    return 0;
}
 